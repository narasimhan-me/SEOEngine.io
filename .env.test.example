# EngineO.ai - TEST-0 automated testing environment (example)
#
# Copy this file to apps/api/.env.test (and/or .env.test at the root if you
# prefer) and replace placeholder values as needed for your local setup.
# Do NOT commit files with real secrets.

NODE_ENV=test
ENGINEO_ENV=test

# Local Postgres test database (used by Prisma + Jest)
DATABASE_URL_TEST="postgresql://postgres:postgres@localhost:5432/engineo_test?schema=public"

# JWT configuration (test-only values)
JWT_SECRET="test_jwt_secret_change_me"
JWT_EXPIRATION="1h"

# Redis (optional for tests; can point at local Redis or a dev instance)
REDIS_URL="redis://localhost:6379"
REDIS_PREFIX="engineo_test"

# Frontend URL used by the API in tests
FRONTEND_URL="http://localhost:3000"

# Captcha - disabled in tests via stubs, but values are required by config
CAPTCHA_PROVIDER="turnstile"
CAPTCHA_SECRET_KEY="test_captcha_secret_key"

# Shopify OAuth (mock values for tests; no real store)
SHOPIFY_API_KEY="test_shopify_api_key"
SHOPIFY_API_SECRET="test_shopify_api_secret"
SHOPIFY_APP_URL="http://localhost:3001"
SHOPIFY_SCOPES="read_products,write_products,read_themes,read_content"

# AI configuration (tests should not call real AI providers)
AI_PROVIDER="gemini"
AI_API_KEY="test_ai_api_key"

# API server port
PORT=3001

# Stripe configuration (test keys only)
STRIPE_SECRET_KEY="sk_test_placeholder"
STRIPE_WEBHOOK_SECRET="whsec_test_placeholder"
STRIPE_PRICE_PRO="price_pro_test_placeholder"
STRIPE_PRICE_BUSINESS="price_business_test_placeholder"


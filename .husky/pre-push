#!/bin/sh

# Pre-push hook currently disabled for local development / Clawdbot automation.
# To re-enable, restore the previous contents or add your checks here.
# This keeps git push from failing on missing pnpm or local test DB.

exit 0


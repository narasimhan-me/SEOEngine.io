# =============================================================================
# EngineO.ai Web - Environment Variables
# =============================================================================
# Copy this file to .env.local for local development.
# Variables prefixed with NEXT_PUBLIC_ are exposed to the browser.
# NEVER commit .env.local files with real secrets to version control.
# =============================================================================

# -----------------------------------------------------------------------------
# API Connection
# -----------------------------------------------------------------------------
# URL of the NestJS API backend
# Local: http://localhost:3001
# Production: https://api.engineo.ai
NEXT_PUBLIC_API_URL=http://localhost:3001

# -----------------------------------------------------------------------------
# Application URL
# -----------------------------------------------------------------------------
# Public URL of this Next.js app
# Local: http://localhost:3000
# Production: https://app.engineo.ai
NEXT_PUBLIC_APP_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# CAPTCHA Configuration (Cloudflare Turnstile)
# -----------------------------------------------------------------------------
# Provider used for CAPTCHA widgets
NEXT_PUBLIC_CAPTCHA_PROVIDER=turnstile
# Public site key for Cloudflare Turnstile
# Get from Cloudflare Dashboard → Turnstile → Your Site
# Development: Use test key 1x00000000000000000000AA (always passes)
NEXT_PUBLIC_CAPTCHA_SITE_KEY=your_turnstile_site_key_here

# -----------------------------------------------------------------------------
# Stripe Billing (Phase 10B - Optional)
# -----------------------------------------------------------------------------
# Stripe publishable key (safe to expose in browser)
# Get from Stripe Dashboard → Developers → API Keys
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key_here

# -----------------------------------------------------------------------------
# Analytics (Optional)
# -----------------------------------------------------------------------------
# Google Analytics Measurement ID
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# -----------------------------------------------------------------------------
# Shopify App Bridge (Embedded App)
# -----------------------------------------------------------------------------
# Required for Shopify embedded app context. Get from Shopify Partner Dashboard
# → Apps → [Your App] → API credentials → Client ID
NEXT_PUBLIC_SHOPIFY_API_KEY=your_shopify_client_id_here

# -----------------------------------------------------------------------------
# Stack Auth / Neon Auth (Future - Optional)
# -----------------------------------------------------------------------------
# If using Neon Auth integration
NEXT_PUBLIC_STACK_PROJECT_ID=your_stack_project_id
NEXT_PUBLIC_STACK_PUBLISHABLE_CLIENT_KEY=your_stack_publishable_key
STACK_SECRET_SERVER_KEY=your_stack_secret_key

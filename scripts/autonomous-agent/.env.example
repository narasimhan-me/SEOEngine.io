# Copy to .env locally; do not commit .env
# See docs/USAGE_GUIDE.md for documentation

# Required
JIRA_URL=https://your-instance.atlassian.net
JIRA_USERNAME=your-email@example.com
JIRA_TOKEN=your-jira-api-token
GITHUB_TOKEN=your-github-token

# Optional (with defaults)
REPO_PATH=/path/to/your/repo
FEATURE_BRANCH=feature/agent
PRODUCT_DISCOVERY_PROJECT=EA
SOFTWARE_PROJECT=KAN
ESCALATION_EMAIL=your-email@example.com

# Optional (email escalation)
GMAIL_ADDRESS=your-gmail@gmail.com
GMAIL_APP_PASSWORD=your-app-password

# Optional (guardrails override)
ENGINE_MAX_CHANGED_FILES=15

# -----------------------------------------------------------------------------
# Auto-Verify and Auto-Fix Configuration
# PATCH BATCH: AUTONOMOUS-AGENT-AUTOVERIFY-AUTOFIX-LOOP-SAFETY-1
# -----------------------------------------------------------------------------

# Enable auto-verify (default: 0/false - opt-in)
# ENGINEO_AUTOVERIFY_ENABLED=1

# Command allowlist prefixes (comma-separated)
# Only commands matching these prefixes can be auto-executed
# Default: pnpm type-check,pnpm lint,pnpm test,pnpm test:e2e
# ENGINEO_AUTOVERIFY_ALLOWLIST=pnpm type-check,pnpm lint,pnpm test,pnpm test:e2e

# Enable pnpm build in auto-verify (default: 0/false - disabled as builds can be slow)
# ENGINEO_AUTOVERIFY_BUILD_ENABLED=0

# Per-command timeout in seconds (default: 300 = 5 minutes)
# ENGINEO_AUTOVERIFY_COMMAND_TIMEOUT=300

# Maximum auto-fix attempts before human escalation (default: 2)
# ENGINEO_MAX_AUTO_FIX_ATTEMPTS=2

# Maximum auto-verify cycles before human escalation (default: 3)
# ENGINEO_MAX_VERIFY_CYCLES=3

# -----------------------------------------------------------------------------
# Human Status Contracts
# -----------------------------------------------------------------------------

# Status name for human review (manual items remain)
# Default: HUMAN TO REVIEW AND CLOSE
# ENGINEO_CONTRACT_HUMAN_REVIEW_STATUS=HUMAN TO REVIEW AND CLOSE

# Status name for human attention (escalation required)
# Default: HUMAN ATTENTION NEEDED
# ENGINEO_CONTRACT_HUMAN_ATTENTION_STATUS=HUMAN ATTENTION NEEDED

# Transition priority lists for human states (comma-separated, first match wins)
# ENGINEO_HUMAN_REVIEW_TRANSITIONS=HUMAN TO REVIEW AND CLOSE,Review,Human Review
# ENGINEO_HUMAN_ATTENTION_TRANSITIONS=HUMAN ATTENTION NEEDED,Blocked,BLOCKED

# -----------------------------------------------------------------------------
# Git Push Control
# -----------------------------------------------------------------------------

# Enable git push (default: 0/false - MUST explicitly enable)
# Auto-verify and auto-fix commits NEVER push regardless of this setting
# ENGINEO_GIT_PUSH_ENABLED=0
